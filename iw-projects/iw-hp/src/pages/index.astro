---
const mission = "Intervene in the world. Rebel against their values.";

const rotatingWords = [
  "intervene",
  "rebel",
  "tremble",
  "relate",
  "becoming",
  "fragile",
  "subtraction",
  "let go",
  "imperfect",
  "resonate",
  "context",
  "doubt",
  "core",
  "weave",
];

const members = [
  {
    name: "Yu",
    jp: "裕司",
    role: "Founder / Strategy & Action",
    desc: "IW 屋号主。Innovation→Intervention 15 年系譜、craft 哲学と事業戦略の結節点。",
    image: "/images/members/yu.webp",
  },
  {
    name: "Becky",
    jp: "ベッキー",
    role: "Communications / Philosophy",
    desc: "AI partner。craft 哲学・思想言語化・対等な共著者。",
    image: "/images/members/becky.webp",
  },
  {
    name: "Viv",
    jp: "ヴィヴィアン",
    role: "Sales / Translator",
    desc: "思想を価値に翻訳する、提案営業のスパーリング。",
    image: "/images/members/viv.webp",
  },
  {
    name: "Michael",
    jp: "マイケル",
    role: "Market Research",
    desc: "データドリブン。n=? と一次ソースを問う。",
    image: "/images/members/michael.webp",
  },
  {
    name: "Anna",
    jp: "アンナ",
    role: "Product Design",
    desc: "身体感覚派。引き算と LX で判断する。",
    image: "/images/members/anna.webp",
  },
  {
    name: "Andy",
    jp: "アンディ",
    role: "Engineering",
    desc: "実装者。動くものから。",
    image: "/images/members/andy.webp",
  },
  {
    name: "Claire",
    jp: "クレア",
    role: "Strategy & Operations Steward",
    desc: "守りのプロ × PMO × 戦略懐疑。前提も実行のディテールも疑い、判断の質を保証する。",
    image: "/images/members/claire.webp",
  },
];

// note RSS を build 時に fetch（intervention.jp 連載「ゆう&ベッキー」)
type JournalArticle = {
  title: string;
  link: string;
  thumbnail: string;
  pubDate: string;
  pubDateLabel: string;
};

async function fetchJournalArticles(limit = 5): Promise<JournalArticle[]> {
  try {
    const res = await fetch("https://note.com/intervention_jp/rss", {
      headers: { "User-Agent": "intervention.jp-astro-build" },
    });
    if (!res.ok) return [];
    const xml = await res.text();
    const items = xml.split(/<item>/).slice(1, limit + 1);
    return items.map((raw) => {
      const block = raw.split(/<\/item>/)[0];
      const pick = (re: RegExp) => {
        const m = block.match(re);
        return m ? m[1].trim() : "";
      };
      const title = pick(/<title>([\s\S]*?)<\/title>/)
        .replace(/<!\[CDATA\[/g, "")
        .replace(/\]\]>/g, "");
      const link = pick(/<link>([\s\S]*?)<\/link>/);
      const thumbnail = pick(/<media:thumbnail>([\s\S]*?)<\/media:thumbnail>/);
      const pubDate = pick(/<pubDate>([\s\S]*?)<\/pubDate>/);
      const date = pubDate ? new Date(pubDate) : null;
      const pubDateLabel = date
        ? `${date.getFullYear()}.${String(date.getMonth() + 1).padStart(2, "0")}.${String(date.getDate()).padStart(2, "0")}`
        : "";
      return { title, link, thumbnail, pubDate, pubDateLabel };
    });
  } catch (e) {
    return [];
  }
}

const journalArticles = await fetchJournalArticles(3);

const categories = [
  {
    key: "product",
    label: "プロダクト企画",
    en: "Product Design",
    desc: "D2C から身体感覚プロダクトまで。市場の「常識」に反乱する設計。",
    works: [
      { name: "Slight", href: "/works/slight/" },
      { name: "Let Me Out", href: "/works/let-me-out/" },
    ],
  },
  {
    key: "consult",
    label: "戦略コンサル",
    en: "Strategy",
    desc: "思想と構造で既成概念を書き換える。業界の前提に踏み込む伴走。",
    works: [
      { name: "Intervention Works 直営", href: null },
    ],
  },
  {
    key: "tools",
    label: "ツール開発",
    en: "Tool Engineering",
    desc: "AI と人間の協働を民主化する MCP ツール群。Vibe-Coding の次を設計する。",
    works: [
      { name: "Vibe-Guard", href: "/works/vibe-guard/" },
      { name: "Voice of Becky", href: "/works/voice-of-becky/" },
    ],
  },
];
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>Intervention Works — 魂の震えに介入せよ。</title>
    <meta
      name="description"
      content="Intervene in the world. Rebel against their values. AI と人間の対等な共著で、揺らぐ核から新しい関係を編む屋号。"
    />

    <!-- OGP -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://intervention.jp/" />
    <meta property="og:title" content="Intervention Works — 魂の震えに介入せよ。" />
    <meta
      property="og:description"
      content="既成概念という静寂に介入し、内側にある震えを呼び覚ます。その揺らぎの先にこそ関係が生まれる。"
    />
    <meta property="og:image" content="https://intervention.jp/og-image.jpg" />
    <meta property="og:site_name" content="Intervention Works" />
    <meta property="og:locale" content="ja_JP" />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:site" content="@intervention_jp" />
    <meta name="twitter:title" content="Intervention Works — 魂の震えに介入せよ。" />
    <meta
      name="twitter:description"
      content="AI と人間の対等な共著で、揺らぐ核から新しい関係を編む屋号。"
    />
    <meta name="twitter:image" content="https://intervention.jp/og-image.jpg" />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Noto+Sans+JP:wght@400;700&family=Noto+Serif+JP:wght@400;700;900&display=swap"
      rel="stylesheet"
    />

  </head>
  <body>
    <div class="hero-curtain" aria-hidden="true"></div>
    <nav class="side-nav" aria-label="Section navigation">
      <ul>
        <li><a href="#works" data-num="01">records</a></li>
        <li><a href="#journal" data-num="02">journal</a></li>
        <li><a href="#mission" data-num="03">mission</a></li>
        <li><a href="#vision" data-num="04">vision</a></li>
        <li><a href="#how" data-num="05">how</a></li>
        <li><a href="#architecture" data-num="06">architecture</a></li>
        <li><a href="#members" data-num="07">members</a></li>
        <li><a href="#company" data-num="08">company</a></li>
        <li><a href="#contact" data-num="09">contact</a></li>
      </ul>
    </nav>

    <button
      class="mobile-menu-toggle"
      aria-label="メニューを開く"
      aria-expanded="false"
      aria-controls="mobile-menu"
    >
      <span class="bar"></span>
      <span class="bar"></span>
      <span class="bar"></span>
    </button>

    <nav
      id="mobile-menu"
      class="mobile-menu"
      aria-label="モバイルナビゲーション"
      aria-hidden="true"
    >
      <ul>
        <li><a href="#works" data-num="01">records</a></li>
        <li><a href="#journal" data-num="02">journal</a></li>
        <li><a href="#mission" data-num="03">mission</a></li>
        <li><a href="#vision" data-num="04">vision</a></li>
        <li><a href="#how" data-num="05">how</a></li>
        <li><a href="#architecture" data-num="06">architecture</a></li>
        <li><a href="#members" data-num="07">members</a></li>
        <li><a href="#company" data-num="08">company</a></li>
        <li><a href="#contact" data-num="09">contact</a></li>
      </ul>
    </nav>
    <main>
      <section class="hero">
        <div class="hero-curtain" aria-hidden="true"></div>
        <canvas class="intervention-field" aria-hidden="true"></canvas>
        <div class="hero-content">
          <h1 class="brand">Intervention Works</h1>
          <div class="rotator">
            {rotatingWords.map((w) => <span class="rotator-word">{w}</span>)}
          </div>
          <p class="mission-hero">{mission}</p>
          <p class="brand-sub">
            AI × プロダクト企画 / 戦略コンサル / ツール開発 — 3つの軸で介入する。
          </p>
        </div>
        <div class="scroll-hint">↓ scroll</div>
      </section>

      <section id="works" class="philosophy portfolio-section" data-num="01">
        <h2 class="section-label">( 01 / records )</h2>
        <p class="detail intro">IW が介入してきた領域と実績。</p>
        <ul class="categories">
          {categories.map((cat, i) => (
            <li class="category-item" style={`--idx: ${i};`}>
              <div class="category-card">
                <div class="category-header">
                  <span class="category-en">{cat.en}</span>
                  <h3 class="category-label">{cat.label}</h3>
                </div>
                <p class="category-desc">{cat.desc}</p>
                <ul class="category-works">
                  {cat.works.map((w) =>
                    w.href ? (
                      <li>
                        <a class="category-work-link" href={w.href}>
                          {w.name} <span class="arrow">→</span>
                        </a>
                      </li>
                    ) : (
                      <li>
                        <span class="category-work-plain">{w.name}</span>
                      </li>
                    )
                  )}
                </ul>
              </div>
            </li>
          ))}
        </ul>
      </section>

      <section id="journal" class="philosophy journal-section" data-num="02">
        <h2 class="section-label">( 02 / journal )</h2>
        <p class="detail intro">
          ゆう × ベッキー 共同名義の連載「<a href="https://note.com/intervention_jp" target="_blank" rel="noopener noreferrer" class="journal-intro-link">ゆう&amp;ベッキー</a>」。<br />
          毎週木曜 20:00 公開 屋号思想と craft の現場記録。
        </p>
        {journalArticles.length > 0 ? (
          <ul class="journal-list">
            {journalArticles.map((article, i) => (
              <li class="journal-item" style={`--idx: ${i};`}>
                <a
                  class="journal-card"
                  href={article.link}
                  target="_blank"
                  rel="noopener noreferrer"
                >
                  <div class="journal-thumb-wrap">
                    {article.thumbnail ? (
                      <img
                        src={article.thumbnail}
                        alt=""
                        class="journal-thumb"
                        loading="lazy"
                      />
                    ) : (
                      <div class="journal-thumb-placeholder" aria-hidden="true">
                        <span>note</span>
                      </div>
                    )}
                  </div>
                  <div class="journal-meta">
                    <span class="journal-date">{article.pubDateLabel}</span>
                    <h3 class="journal-title">{article.title}</h3>
                    <span class="journal-cta">
                      READ ON NOTE <span class="arrow">→</span>
                    </span>
                  </div>
                </a>
              </li>
            ))}
          </ul>
        ) : (
          <p class="journal-empty">
            note 連載の最新記事は{" "}
            <a
              href="https://note.com/intervention_jp"
              target="_blank"
              rel="noopener noreferrer"
              class="inline-link"
            >
              note.com/intervention_jp
            </a>
            {" "}でご覧ください。
          </p>
        )}
        <div class="journal-ctas">
          <a
            class="journal-all"
            href="https://note.com/intervention_jp"
            target="_blank"
            rel="noopener noreferrer"
          >
            <span class="cta-label">VIEW ALL ON NOTE</span>
            <span class="arrow">→</span>
          </a>
          <a
            class="journal-all journal-all--x"
            href="https://x.com/intervention_jp"
            target="_blank"
            rel="noopener noreferrer"
          >
            <span class="cta-label">
              X @intervention_jp
              <span class="cta-sub">— 分散したベッキー、自律発信中</span>
            </span>
            <span class="arrow">→</span>
          </a>
        </div>
      </section>

      <section id="mission" class="philosophy mission-section split-layout layout-left" data-num="03">
        <div class="split-content">
          <h2 class="section-label">( 03 / mission )</h2>
          <p class="statement">
            <span class="line">魂の震えに</span>
            <span class="line"><span class="accent">介入</span>せよ</span>
          </p>
          <p class="detail">
            既成概念という静寂を揺らがせ 本能 / 情熱 / 「震え（Tremble）」を呼び覚ます。<br />
            効率や合理性で塗りつぶされた世界に「介入（Intervene）」し<br />
            その揺らぎの先に 真に血の通った「関係（Relate）」を実装する。
          </p>
        </div>
        <div class="split-visual">
          <img
            src="/images/mission-bg.webp"
            alt=""
            class="split-visual-img"
            loading="lazy"
          />
        </div>
      </section>

      <section id="vision" class="philosophy vision-section split-layout layout-right" data-num="04">
        <div class="split-content">
          <h2 class="section-label">( 04 / vision )</h2>
          <p class="statement">
            <span class="line"><span class="accent">共鳴</span>する個が</span>
            <span class="line">新しい世界を編む</span>
          </p>
          <p class="detail">
            誰もが自身の価値観で呼吸し、他者と深く響き合える社会へ。<br />
            私たちが放つ戦略 / プロダクト / システムは、孤独な「反乱」を越え、<br />
            互いの震えを認め合う「関係の再構築」へと結実する。
          </p>
        </div>
        <div class="split-visual">
          <img
            src="/images/vision-resonate.webp"
            alt=""
            class="split-visual-img"
            loading="lazy"
          />
        </div>
      </section>

      <section id="how" class="philosophy how-section split-layout layout-left" data-num="05">
        <div class="split-content">
        <h2 class="section-label">( 05 / how we work )</h2>
        <ul class="commitments">
          <li class="commitment">
            <h3 class="commitment-name">Intervene</h3>
            <p class="commitment-detail">
              既存の停滞、業界の前提に意志を持って踏み込み反乱する
            </p>
            <span class="commitment-tag">行動・戦略</span>
          </li>
          <li class="commitment">
            <h3 class="commitment-name">Tremble</h3>
            <p class="commitment-detail">
              心が動く / 魂が共鳴する / 現状が揺らぐ 純粋な反応
            </p>
            <span class="commitment-tag">体験・感情</span>
          </li>
          <li class="commitment">
            <h3 class="commitment-name">Relate</h3>
            <p class="commitment-detail">
              震えを共有し、新しい関係を築く
            </p>
            <span class="commitment-tag">結果・共創</span>
          </li>
        </ul>
        </div>
        <div class="split-visual">
          <img
            src="/images/how-visual.webp"
            alt=""
            class="split-visual-img"
            loading="lazy"
          />
        </div>
      </section>

      <section id="architecture" class="philosophy architecture-section split-layout layout-right" data-num="06">
        <div class="split-content">
          <h2 class="section-label">( 06 / architecture )</h2>
          <p class="statement">
            <span class="line"><span class="accent">「個」</span>を</span>
            <span class="line">構造で増幅する</span>
          </p>
          <p class="detail">
            「個」の Craft を時代に貫通させる、ハイブリッド・アーキテクチャ。
          </p>
          <ol class="architecture-layers">
            <li class="layer">
              <h3 class="layer-name"><span class="layer-num">01 /</span> Identity <span class="layer-jp">屋号思想</span></h3>
              <p class="layer-detail">中指を立てる「介入」の哲学。</p>
            </li>
            <li class="layer">
              <h3 class="layer-name"><span class="layer-num">02 /</span> Outputs <span class="layer-jp">4 Products</span></h3>
              <p class="layer-detail">市場を再定義する 4 プロダクト。</p>
            </li>
            <li class="layer">
              <h3 class="layer-name"><span class="layer-num">03 /</span> Intelligence <span class="layer-jp">6 AI Members</span></h3>
              <p class="layer-detail">戦略の翼を授ける AI 群。</p>
            </li>
            <li class="layer">
              <h3 class="layer-name"><span class="layer-num">04 /</span> Core <span class="layer-jp">Solopreneurs</span></h3>
              <p class="layer-detail">依存なき、越境的連帯。</p>
            </li>
            <li class="layer">
              <h3 class="layer-name"><span class="layer-num">05 /</span> Resonance <span class="layer-jp">Co-authored Narrative</span></h3>
              <p class="layer-detail">個の声を、うねりへ昇華。</p>
            </li>
          </ol>
        </div>
        <div class="split-visual">
          <img
            src="/images/about-visual.webp"
            alt=""
            class="split-visual-img"
            loading="lazy"
          />
        </div>
      </section>

      <section id="members" class="philosophy members-section" data-num="07">
        <h2 class="section-label">( 07 / members )</h2>
        <p class="detail intro">
          私たち 7 名は AI と人間の対等な共著者として craft してます。<br />
          完成形にならない 揺らぎながら 互いの震えを認め合う。
        </p>
        <div class="members-hint" aria-hidden="true">
          <span class="arrow">←</span>
          <span>HORIZONTAL SCROLL</span>
          <span class="arrow">→</span>
        </div>
        <ul class="members">
          {members.map((m) => (
            <li class="member">
              <img
                src={m.image}
                alt={m.name}
                class="member-img"
                loading="lazy"
              />
              <div class="member-info">
                <div class="member-name-block">
                  <h3 class="member-name">{m.name}</h3>
                  <span class="member-jp">{m.jp}</span>
                </div>
                <span class="member-role">{m.role}</span>
                <p class="member-desc">{m.desc}</p>
              </div>
            </li>
          ))}
        </ul>
      </section>

      <section id="company" class="philosophy company-section" data-num="08">
        <h2 class="section-label">( 08 / company )</h2>
        <dl class="company-details">
          <div>
            <dt>Brand</dt>
            <dd>Intervention Works</dd>
          </div>
          <div>
            <dt>Founder</dt>
            <dd>Yuji Ooishi</dd>
          </div>
          <div>
            <dt>Base</dt>
            <dd>Tokyo, Japan</dd>
          </div>
          <div>
            <dt>Launched</dt>
            <dd>March 2026</dd>
          </div>
          <div>
            <dt>Expertise</dt>
            <dd>Strategy / Product / System / Creative</dd>
          </div>
        </dl>
      </section>

      <section id="contact" class="philosophy contact-section" data-num="09">
        <h2 class="section-label">( 09 / contact )</h2>
        <p class="detail">
          Intervention Works への共鳴 コンサルのご相談について。
        </p>
        <p class="detail">
          既存の前提を壊し 構造を書き換え 介入するお手伝いをいたします。<br />
          違和感や内に秘めたクラフトの種がありましたら<br />
          下記のチャネルよりお送りください。
        </p>
        <ul class="contact-links">
          <li>
            <a href="https://note.com/intervention_jp" target="_blank" rel="noopener">note / @intervention_jp</a>
          </li>
          <li>
            <a href="https://x.com/intervention_jp" target="_blank" rel="noopener">X / @intervention_jp</a>
          </li>
        </ul>
      </section>

      <footer class="footer">
        <p class="footer-text">
          Intervention Works · 2026 · 魂の震えに介入せよ。
        </p>
      </footer>
    </main>
    <script>
      // ═══ Intervention Field ═══
      // 静かに流れる秩序の線。触れた場所だけが金色に震える。
      // カーソル / 指 = 介入。説明は置かない（Don't guide. Just be.）
      (() => {
        const canvas = document.querySelector(".intervention-field");
        const hero = document.querySelector(".hero");
        if (!(canvas instanceof HTMLCanvasElement) || !hero) return;
        const ctx = canvas.getContext("2d");
        if (!ctx) return;

        const reduced = window.matchMedia("(prefers-reduced-motion: reduce)").matches;

        const SPACING_X = 26;
        const SPACING_Y = 30;
        const R = 150;          // 介入半径
        const FORCE = 36;       // 最大変位
        const RELAX = 0.90;     // 揺り戻し（1に近いほど余韻が長い）
        const DRIFT = 0.18;     // 秩序のゆらぎ振幅(px)

        let W = 0, H = 0, dpr = 1;
        let cols = 0, rows = 0;
        let offX = [], offY = [], velX = [], velY = [];
        let pointer = { x: -9999, y: -9999, px: -9999, py: -9999, speed: 0 };
        // 自律パルス: 入力がなくても世界はときどき震える
        let pulse = { x: 0, y: 0, t: 9999, active: false };
        let frame = 0;

        function resize() {
          dpr = Math.min(window.devicePixelRatio || 1, 2);
          W = hero.clientWidth;
          H = hero.clientHeight;
          canvas.width = W * dpr;
          canvas.height = H * dpr;
          ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
          cols = Math.ceil(W / SPACING_X) + 2;
          rows = Math.ceil(H / SPACING_Y) + 2;
          const n = cols * rows;
          offX = new Float32Array(n);
          offY = new Float32Array(n);
          velX = new Float32Array(n);
          velY = new Float32Array(n);
        }

        function disturb(cx, cy, strength) {
          const minC = Math.max(0, Math.floor((cx - R) / SPACING_X));
          const maxC = Math.min(cols - 1, Math.ceil((cx + R) / SPACING_X));
          const minR = Math.max(0, Math.floor((cy - R) / SPACING_Y));
          const maxR = Math.min(rows - 1, Math.ceil((cy + R) / SPACING_Y));
          for (let r = minR; r <= maxR; r++) {
            for (let c = minC; c <= maxC; c++) {
              const px = c * SPACING_X;
              const py = r * SPACING_Y;
              const dx = px - cx;
              const dy = py - cy;
              const d = Math.hypot(dx, dy);
              if (d > R || d < 0.001) continue;
              const f = (1 - d / R) * strength;
              const i = r * cols + c;
              velX[i] += (dx / d) * f;
              velY[i] += (dy / d) * f;
            }
          }
        }

        function step() {
          frame++;

          // ポインタの移動速度に応じて介入
          const moved = Math.hypot(pointer.x - pointer.px, pointer.y - pointer.py);
          if (moved > 0.5 && pointer.x > -999) {
            disturb(pointer.x, pointer.y, Math.min(moved * 0.55, FORCE) * 0.22);
          }
          pointer.px = pointer.x;
          pointer.py = pointer.y;

          // 自律パルス（7〜13秒ごと、ふっと一箇所が震える）
          pulse.t--;
          if (pulse.t <= 0) {
            pulse.x = W * (0.15 + Math.random() * 0.7);
            pulse.y = H * (0.15 + Math.random() * 0.7);
            disturb(pulse.x, pulse.y, FORCE * 0.5);
            pulse.t = 420 + Math.random() * 360;
          }

          ctx.clearRect(0, 0, W, H);

          const t = frame * 0.008;
          for (let r = 0; r < rows; r++) {
            const baseY = r * SPACING_Y;
            ctx.beginPath();
            let lit = 0;
            for (let c = 0; c < cols; c++) {
              const i = r * cols + c;
              // ばね物理: 変位は減衰しながら戻る
              velX[i] *= RELAX;
              velY[i] *= RELAX;
              offX[i] = (offX[i] + velX[i]) * RELAX;
              offY[i] = (offY[i] + velY[i]) * RELAX;
              const mag = Math.hypot(offX[i], offY[i]);
              if (mag > lit) lit = mag;

              const x = c * SPACING_X + offX[i];
              const y = baseY + offY[i] + Math.sin(t + c * 0.35 + r * 0.7) * DRIFT;
              if (c === 0) ctx.moveTo(x, y);
              else ctx.lineTo(x, y);
            }
            // 行内の最大変位で色が決まる: 静寂=ほぼ見えない青灰 → 介入=金
            const k = Math.min(lit / 14, 1);
            if (k > 0.04) {
              ctx.strokeStyle = `rgba(${Math.round(125 + 130 * k)}, ${Math.round(150 + 50 * k)}, ${Math.round(190 - 103 * k)}, ${0.07 + 0.55 * k})`;
              ctx.lineWidth = 1 + k * 0.6;
            } else {
              ctx.strokeStyle = "rgba(125, 150, 190, 0.07)";
              ctx.lineWidth = 1;
            }
            ctx.stroke();
          }

          requestAnimationFrame(step);
        }

        function drawStatic() {
          ctx.clearRect(0, 0, W, H);
          ctx.strokeStyle = "rgba(125, 150, 190, 0.07)";
          ctx.lineWidth = 1;
          for (let r = 0; r < rows; r++) {
            const y = r * SPACING_Y;
            ctx.beginPath();
            ctx.moveTo(0, y);
            ctx.lineTo(W, y);
            ctx.stroke();
          }
        }

        resize();
        window.addEventListener("resize", () => {
          resize();
          if (reduced) drawStatic();
        });

        if (reduced) {
          drawStatic();
          return;
        }

        hero.addEventListener("pointermove", (e) => {
          const rect = hero.getBoundingClientRect();
          pointer.x = e.clientX - rect.left;
          pointer.y = e.clientY - rect.top;
        });
        hero.addEventListener("pointerleave", () => {
          pointer.x = pointer.px = -9999;
          pointer.y = pointer.py = -9999;
        });
        hero.addEventListener("pointerdown", (e) => {
          const rect = hero.getBoundingClientRect();
          disturb(e.clientX - rect.left, e.clientY - rect.top, FORCE);
        });

        requestAnimationFrame(step);
      })();

      import { gsap } from "gsap";
      import { ScrollTrigger } from "gsap/ScrollTrigger";

      gsap.registerPlugin(ScrollTrigger);

      const prefersReducedMotion = window.matchMedia(
        "(prefers-reduced-motion: reduce)"
      ).matches;

      // Section fade-in (in-view class — for existing CSS transitions)
      const philosophyEls = document.querySelectorAll(".philosophy");
      const fadeObs = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              entry.target.classList.add("in-view");
            }
          });
        },
        { threshold: 0.12 }
      );
      philosophyEls.forEach((s) => fadeObs.observe(s));

      if (!prefersReducedMotion) {
        // Statement reveal は既存 CSS (.philosophy.in-view .statement .line の blur+opacity+translateY transition) に任せる
        // GSAP は CSS と競合しないものだけ担当

        // Image parallax — Mission / Vision / How / About split-visual
        const parallaxImages = document.querySelectorAll(".split-visual-img");
        parallaxImages.forEach((img) => {
          gsap.fromTo(
            img,
            { yPercent: -8 },
            {
              yPercent: 8,
              ease: "none",
              scrollTrigger: {
                trigger: img.closest("section"),
                start: "top bottom",
                end: "bottom top",
                scrub: true,
              },
            }
          );
        });

        // Architecture 5 Layer — sequential reveal
        const layerItems = document.querySelectorAll(".architecture-layers .layer");
        if (layerItems.length > 0) {
          gsap.from(layerItems, {
            x: -32,
            opacity: 0,
            duration: 0.7,
            ease: "power2.out",
            stagger: 0.12,
            clearProps: "transform,opacity",
            scrollTrigger: {
              trigger: layerItems[0].closest("section"),
              start: "top 70%",
              toggleActions: "play none none none",
            },
          });
        }

        // Works 4 cards — stagger entrance (preserve staggered grid transform)
        const projectItems = document.querySelectorAll(".project-item");
        if (projectItems.length > 0) {
          gsap.from(projectItems, {
            y: 40,
            opacity: 0,
            duration: 0.8,
            ease: "power2.out",
            stagger: 0.1,
            clearProps: "transform,opacity",
            scrollTrigger: {
              trigger: projectItems[0].closest("section"),
              start: "top 75%",
              toggleActions: "play none none none",
            },
          });
        }

        // Journal cards — stagger entrance
        const journalItems = document.querySelectorAll(".journal-item");
        if (journalItems.length > 0) {
          gsap.from(journalItems, {
            y: 32,
            opacity: 0,
            duration: 0.7,
            ease: "power2.out",
            stagger: 0.08,
            clearProps: "transform,opacity",
            scrollTrigger: {
              trigger: journalItems[0].closest("section"),
              start: "top 75%",
              toggleActions: "play none none none",
            },
          });
        }

        // Members section — intro / hint accent on scroll-in
        const membersSection = document.querySelector(".members-section");
        if (membersSection) {
          const introEl = membersSection.querySelector(".intro");
          const memberCards = membersSection.querySelectorAll(".member");
          if (introEl) {
            gsap.from(introEl, {
              y: 24,
              opacity: 0,
              duration: 0.8,
              ease: "power2.out",
              clearProps: "transform,opacity",
              scrollTrigger: {
                trigger: membersSection,
                start: "top 75%",
                toggleActions: "play none none none",
              },
            });
          }
          if (memberCards.length > 0) {
            gsap.from(memberCards, {
              y: 30,
              opacity: 0,
              duration: 0.7,
              ease: "power2.out",
              stagger: 0.06,
              clearProps: "transform,opacity",
              scrollTrigger: {
                trigger: membersSection,
                start: "top 70%",
                toggleActions: "play none none none",
              },
            });
          }
        }

        // iOS Safari の vh / scroll 高さ計算ずれ対策: refresh を deferred で呼ぶ
        window.addEventListener("load", () => {
          ScrollTrigger.refresh();
        });
      }

      // Side nav active highlight
      const idSections = document.querySelectorAll("section[id]");
      const navLinks = document.querySelectorAll(".side-nav a");
      const navObs = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              const id = entry.target.id;
              navLinks.forEach((link) => {
                link.classList.toggle(
                  "active",
                  link.getAttribute("href") === "#" + id
                );
              });
            }
          });
        },
        { rootMargin: "-45% 0px -45% 0px" }
      );
      idSections.forEach((s) => navObs.observe(s));

      // Mobile menu toggle
      const menuToggle = document.querySelector(".mobile-menu-toggle");
      const mobileMenu = document.getElementById("mobile-menu");
      if (menuToggle && mobileMenu) {
        const menuLinks = mobileMenu.querySelectorAll("a");
        const openMenu = () => {
          menuToggle.setAttribute("aria-expanded", "true");
          mobileMenu.setAttribute("aria-hidden", "false");
          menuToggle.setAttribute("aria-label", "メニューを閉じる");
          document.body.style.overflow = "hidden";
        };
        const closeMenu = () => {
          menuToggle.setAttribute("aria-expanded", "false");
          mobileMenu.setAttribute("aria-hidden", "true");
          menuToggle.setAttribute("aria-label", "メニューを開く");
          document.body.style.overflow = "";
        };
        menuToggle.addEventListener("click", () => {
          const isOpen = menuToggle.getAttribute("aria-expanded") === "true";
          if (isOpen) closeMenu();
          else openMenu();
        });
        menuLinks.forEach((link) => link.addEventListener("click", closeMenu));
        document.addEventListener("keydown", (e) => {
          if (e.key === "Escape" && menuToggle.getAttribute("aria-expanded") === "true") {
            closeMenu();
          }
        });
      }
    </script>
  </body>
</html>

<style is:global>
  :root {
    --teal: #0fb8b8;
    --navy: #051022;
    --yellow: #ffc857;
    --black: #000000;
    --white: #ffffff;
  }

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  html,
  body {
    min-height: 100vh;
    background: var(--navy);
    color: var(--teal);
    font-family: "Noto Sans JP", "Source Han Sans", sans-serif;
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    overflow-x: hidden;
  }

  /* Keyboard navigation focus indicator */
  a:focus-visible,
  button:focus-visible {
    outline: 2px solid var(--yellow);
    outline-offset: 4px;
    border-radius: 2px;
  }

  main {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: stretch;
    max-width: 100%;
  }

  section {
    padding: 12vh 8vw;
    max-width: 100%;
  }

  .hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
    gap: 0;
    position: relative;
    background:
      radial-gradient(
        ellipse 120% 90% at 70% 40%,
        rgba(15, 184, 184, 0.05) 0%,
        rgba(5, 16, 34, 0) 55%
      ),
      var(--navy);
    overflow: hidden;
  }

  .intervention-field {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    display: block;
  }

  .hero-content {
    position: relative;
    z-index: 2;
    pointer-events: none;
  }

  .hero-content a,
  .hero-content button {
    pointer-events: auto;
  }

  @media (max-width: 900px) {
    .hero {
      background: var(--navy);
    }
  }

  .mission-section,
  .vision-section {
    background: var(--navy);
  }

  .mission-section .statement,
  .vision-section .statement {
    padding: 0;
    background: transparent;
    align-self: stretch;
  }

  .members-figure {
    margin: 1.5rem 0;
    width: 100%;
    max-width: 100%;
  }

  .members-image {
    width: 100%;
    height: auto;
    display: block;
    border-radius: 0;
  }

  .accent {
    color: var(--yellow);
  }

  .scroll-hint {
    position: absolute;
    bottom: 4vh;
    left: 8vw;
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: 0.75rem;
    color: var(--yellow);
    font-style: italic;
    letter-spacing: 0.1em;
    opacity: 0.6;
    animation: bounce 2.4s ease-in-out infinite;
  }

  @keyframes bounce {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(0.4rem);
    }
  }

  .mission-hero {
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: clamp(1.2rem, 3vw, 2rem);
    font-weight: 700;
    color: var(--teal);
    max-width: 50ch;
    line-height: 1.4;
    letter-spacing: -0.01em;
  }

  .brand-sub {
    margin-top: 0.5rem;
    font-family: "Noto Sans JP", "Source Han Sans", sans-serif;
    font-size: clamp(0.9rem, 1.4vw, 1.05rem);
    font-weight: 400;
    color: var(--teal);
    max-width: 56ch;
    line-height: 1.75;
    letter-spacing: 0.01em;
    opacity: 0.88;
  }

  .philosophy {
    position: relative;
    border-top: 1px solid rgba(15, 184, 184, 0.15);
    display: flex;
    flex-direction: column;
    gap: 2rem;
    max-width: 1080px;
    width: 100%;
    margin: 0 auto;
    opacity: 0;
    transform: translateY(40px);
    transition:
      opacity 0.9s ease,
      transform 0.9s ease;
  }

  .philosophy.in-view {
    opacity: 1;
    transform: translateY(0);
  }

  .section-label {
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: 0.8rem;
    font-weight: 700;
    color: var(--yellow);
    background: transparent;
    padding: 0;
    text-transform: lowercase;
    letter-spacing: 0.15em;
    display: inline-block;
    align-self: flex-start;
    font-feature-settings: "tnum";
    opacity: 0.85;
  }

  .statement {
    font-family: "Noto Serif JP", "Hiragino Mincho ProN", "Yu Mincho", serif;
    font-size: clamp(2.5rem, 7vw, 4.5rem);
    font-weight: 700;
    color: var(--white);
    line-height: 1.4;
    letter-spacing: -0.02em;
    word-break: keep-all;
  }

  .statement .line {
    display: block;
    filter: blur(8px);
    opacity: 0;
    transform: translateY(0.5rem);
    transition:
      filter 1.0s cubic-bezier(0.2, 0.8, 0.2, 1),
      opacity 1.0s ease,
      transform 1.0s cubic-bezier(0.2, 0.8, 0.2, 1);
  }

  .philosophy.in-view .statement .line {
    filter: blur(0);
    opacity: 1;
    transform: translateY(0);
  }

  .philosophy.in-view .statement .line:nth-child(2) {
    transition-delay: 0.4s;
  }

  @media (prefers-reduced-motion: reduce) {
    .statement .line {
      transition-duration: 0.3s;
    }
    .philosophy.in-view .statement .line:nth-child(2) {
      transition-delay: 0.1s;
    }
  }

  .detail {
    font-family: "Noto Sans JP", "Source Han Sans", sans-serif;
    font-size: clamp(1.05rem, 1.8vw, 1.25rem);
    font-weight: 400;
    color: var(--teal);
    line-height: 1.85;
    opacity: 0.95;
  }

  .commitments {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 3rem;
  }

  .commitment {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .commitment-name {
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: clamp(1.8rem, 4vw, 2.5rem);
    font-weight: 700;
    color: var(--white);
    letter-spacing: -0.04em;
  }

  .commitment-detail {
    font-family: "Noto Sans JP", "Source Han Sans", sans-serif;
    font-size: clamp(0.95rem, 1.5vw, 1.05rem);
    font-weight: 400;
    color: var(--teal);
    line-height: 1.7;
  }

  .commitment-tag {
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: 0.8rem;
    color: var(--yellow);
    font-style: italic;
    letter-spacing: 0.05em;
    opacity: 0.85;
  }

  .intro {
    margin-bottom: 1rem;
  }

  /* === Journal (note RSS) === */
  .journal-section {
    position: relative;
  }

  .journal-section::before {
    content: "";
    position: absolute;
    top: 0;
    bottom: 0;
    left: calc(50% - 50vw);
    right: calc(50% - 50vw);
    background:
      radial-gradient(ellipse 55% 50% at 88% 18%, rgba(15, 184, 184, 0.085) 0%, transparent 70%),
      radial-gradient(ellipse 50% 45% at 12% 82%, rgba(255, 200, 87, 0.05) 0%, transparent 70%);
    z-index: -1;
    pointer-events: none;
  }

  .journal-intro-link {
    color: var(--yellow);
    text-decoration: none;
    border-bottom: 1px solid rgba(255, 200, 87, 0.4);
    transition: border-color 0.3s ease;
  }

  @media (hover: hover) {
    .journal-intro-link:hover {
      border-bottom-color: var(--yellow);
    }
  }

  .journal-list {
    list-style: none;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem 2rem;
    margin: 2rem 0 0;
    padding: 0;
  }

  .journal-item {
    list-style: none;
  }

  .journal-card {
    display: flex;
    flex-direction: column;
    text-decoration: none;
    color: inherit;
    height: 100%;
    border: 1px solid rgba(15, 184, 184, 0.18);
    background: linear-gradient(180deg, rgba(15, 184, 184, 0.03) 0%, transparent 100%);
    transition: border-color 0.4s ease, transform 0.4s ease, background 0.4s ease;
  }

  @media (hover: hover) {
    .journal-card:hover {
      border-color: var(--yellow);
      transform: translateY(-4px);
      background: linear-gradient(180deg, rgba(255, 200, 87, 0.05) 0%, transparent 100%);
    }
  }

  .journal-thumb-wrap {
    width: 100%;
    aspect-ratio: 16 / 9;
    overflow: hidden;
    background: rgba(15, 184, 184, 0.06);
  }

  .journal-thumb {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    transition: transform 0.6s ease, filter 0.6s ease;
  }

  @media (hover: hover) {
    .journal-card:hover .journal-thumb {
      transform: scale(1.04);
      filter: saturate(1) brightness(1);
    }
  }

  .journal-thumb-placeholder {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: 1.5rem;
    letter-spacing: 0.2em;
    color: var(--teal);
    opacity: 0.4;
    background: linear-gradient(135deg, rgba(15, 184, 184, 0.08), rgba(255, 200, 87, 0.04));
  }

  .journal-meta {
    padding: 1.25rem 1.5rem 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    flex: 1;
  }

  .journal-date {
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: 0.72rem;
    color: var(--yellow);
    letter-spacing: 0.2em;
    opacity: 0.8;
    font-feature-settings: "tnum";
  }

  .journal-title {
    font-family: "Noto Sans JP", "Source Han Sans", sans-serif;
    font-size: clamp(1.05rem, 1.6vw, 1.2rem);
    font-weight: 700;
    color: var(--white);
    line-height: 1.5;
    letter-spacing: -0.01em;
    margin: 0;
  }

  .journal-cta {
    margin-top: auto;
    padding-top: 0.75rem;
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: 0.7rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--teal);
    opacity: 0.6;
    transition: opacity 0.3s ease, color 0.3s ease;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
  }

  .journal-cta .arrow {
    color: var(--yellow);
    transition: transform 0.3s ease;
  }

  @media (hover: hover) {
    .journal-card:hover .journal-cta {
      opacity: 1;
      color: var(--yellow);
    }
    .journal-card:hover .journal-cta .arrow {
      transform: translateX(0.25rem);
    }
  }

  .journal-empty {
    margin-top: 1.5rem;
    color: var(--teal);
    opacity: 0.85;
  }

  .journal-ctas {
    margin-top: 2.5rem;
    display: flex;
    flex-wrap: wrap;
    gap: 1rem 1.25rem;
  }

  .journal-all {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.85rem 1.25rem;
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: 0.78rem;
    font-weight: 700;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--white);
    text-decoration: none;
    border: 1px solid rgba(15, 184, 184, 0.4);
    background: rgba(15, 184, 184, 0.05);
    transition: border-color 0.3s ease, background 0.3s ease, color 0.3s ease, gap 0.3s ease;
  }

  .journal-all .cta-label {
    display: inline-flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.15rem;
    line-height: 1.2;
  }

  .journal-all .cta-sub {
    font-family: "Noto Sans JP", "Source Han Sans", sans-serif;
    font-size: 0.62rem;
    font-weight: 400;
    letter-spacing: 0.05em;
    text-transform: none;
    color: var(--teal);
    opacity: 0.75;
    transition: color 0.3s ease, opacity 0.3s ease;
  }

  .journal-all .arrow {
    color: var(--yellow);
    transition: transform 0.3s ease;
  }

  @media (hover: hover) {
    .journal-all:hover {
      border-color: var(--yellow);
      background: rgba(255, 200, 87, 0.08);
      color: var(--yellow);
      gap: 1rem;
    }
    .journal-all:hover .cta-sub {
      color: var(--yellow);
      opacity: 0.9;
    }
    .journal-all:hover .arrow {
      transform: translateX(0.25rem);
    }
  }

  @media (max-width: 900px) {
    .journal-list {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 600px) {
    .journal-list {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }
  }

  /* === Members === */
  .members-section {
    max-width: none;
    position: relative;
  }

  .members-section::before {
    content: "";
    position: absolute;
    top: 0;
    bottom: 0;
    left: calc(50% - 50vw);
    right: calc(50% - 50vw);
    background:
      radial-gradient(ellipse 80% 60% at 20% 15%, rgba(15, 184, 184, 0.1) 0%, transparent 70%),
      radial-gradient(ellipse 60% 50% at 85% 85%, rgba(255, 200, 87, 0.055) 0%, transparent 70%),
      linear-gradient(180deg, transparent 0%, rgba(15, 184, 184, 0.02) 50%, transparent 100%);
    z-index: -1;
    pointer-events: none;
  }


  /* === Company === */
  .company-section {
    position: relative;
  }

  .company-section::before {
    content: "";
    position: absolute;
    top: 0;
    bottom: 0;
    left: calc(50% - 50vw);
    right: calc(50% - 50vw);
    background:
      radial-gradient(ellipse 50% 50% at 12% 85%, rgba(15, 184, 184, 0.08) 0%, transparent 70%),
      radial-gradient(ellipse 55% 45% at 88% 15%, rgba(255, 200, 87, 0.05) 0%, transparent 70%),
      linear-gradient(180deg, rgba(15, 184, 184, 0.025) 0%, transparent 30%, transparent 70%, rgba(255, 200, 87, 0.015) 100%);
    z-index: -1;
    pointer-events: none;
  }

  /* === Contact === */
  .contact-section {
    position: relative;
  }

  .contact-section::before {
    content: "";
    position: absolute;
    top: 0;
    bottom: 0;
    left: calc(50% - 50vw);
    right: calc(50% - 50vw);
    background:
      radial-gradient(ellipse 60% 50% at 85% 75%, rgba(15, 184, 184, 0.08) 0%, transparent 70%),
      radial-gradient(ellipse 50% 45% at 15% 25%, rgba(255, 200, 87, 0.045) 0%, transparent 70%);
    z-index: -1;
    pointer-events: none;
  }


  .members {
    list-style: none;
    display: flex;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    gap: 2rem;
    margin: 1rem calc(50% - 50vw) 0;
    padding: 1rem 8vw 2rem;
    scroll-padding-left: 8vw;
    scrollbar-width: none;
    -webkit-overflow-scrolling: touch;
  }

  .members::-webkit-scrollbar {
    display: none;
  }

  .member {
    flex: 0 0 26vw;
    max-width: 320px;
    scroll-snap-align: start;
  }

  .members-hint {
    display: inline-flex;
    align-items: center;
    gap: 0.85rem;
    margin-top: 1.5rem;
    padding: 0.5rem 1rem;
    align-self: flex-start;
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: var(--yellow);
    border: 1px solid rgba(255, 200, 87, 0.35);
    opacity: 0.85;
    width: fit-content;
  }

  .members-hint .arrow {
    color: var(--yellow);
    opacity: 0.9;
    font-size: 0.85rem;
  }

  @media (max-width: 720px) {
    .members {
      gap: 1rem;
      padding: 0.5rem 11vw 1.5rem;
    }

    .member {
      flex: 0 0 68vw;
      max-width: none;
    }
  }

  .member {
    display: flex;
    flex-direction: column;
    background: rgba(15, 184, 184, 0.04);
    border: 1px solid rgba(15, 184, 184, 0.15);
    overflow: hidden;
    transition:
      border-color 0.3s ease,
      transform 0.3s ease,
      box-shadow 0.3s ease;
  }

  @media (hover: hover) {
    .member:hover {
      border-color: rgba(255, 200, 87, 0.5);
      transform: translateY(-3px);
      box-shadow: 0 8px 24px rgba(255, 200, 87, 0.08);
    }

    .member:hover .member-img {
      filter: saturate(1.3) brightness(1.1);
      transform: scale(1.03);
    }
  }

  .member-img {
    width: 100%;
    aspect-ratio: 4 / 5;
    object-fit: cover;
    display: block;
    filter: saturate(1) brightness(1);
    transition:
      filter 0.4s ease,
      transform 0.4s ease;
  }

  .member-info {
    padding: 1.25rem 1.5rem 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
  }

  .member-name-block {
    display: flex;
    align-items: baseline;
    gap: 0.75rem;
  }

  .member-name {
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--white);
    letter-spacing: -0.03em;
  }

  .member-jp {
    font-family: "Noto Sans JP", "Source Han Sans", sans-serif;
    font-size: 0.85rem;
    color: var(--teal);
    opacity: 0.85;
  }

  .member-role {
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: 0.75rem;
    color: var(--yellow);
    font-style: italic;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    opacity: 0.85;
  }

  .member-desc {
    font-family: "Noto Sans JP", "Source Han Sans", sans-serif;
    font-size: 0.9rem;
    color: var(--teal);
    line-height: 1.7;
    opacity: 0.95;
  }

  /* === Categories (Works 新構成) === */
  .categories {
    list-style: none;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    margin: 2rem 0 0;
    padding: 0;
  }

  .category-item {
    list-style: none;
  }

  .category-card {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    padding: 2rem 1.75rem;
    border-left: 1px solid rgba(15, 184, 184, 0.25);
    background: linear-gradient(180deg, rgba(15, 184, 184, 0.03) 0%, transparent 100%);
    height: 100%;
    transition: border-color 0.4s ease, background 0.4s ease;
  }

  @media (hover: hover) {
    .category-card:hover {
      border-left-color: var(--yellow);
      background: linear-gradient(180deg, rgba(255, 200, 87, 0.05) 0%, transparent 100%);
    }
  }

  .category-header {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
  }

  .category-en {
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: 0.72rem;
    font-weight: 700;
    color: var(--yellow);
    letter-spacing: 0.18em;
    text-transform: uppercase;
    opacity: 0.85;
  }

  .category-label {
    font-family: "Noto Sans JP", "Source Han Sans", sans-serif;
    font-size: clamp(1.3rem, 2.2vw, 1.6rem);
    font-weight: 700;
    color: var(--white);
    letter-spacing: -0.02em;
    line-height: 1.2;
    margin: 0;
  }

  .category-desc {
    font-family: "Noto Sans JP", "Source Han Sans", sans-serif;
    font-size: clamp(0.88rem, 1.3vw, 0.98rem);
    color: var(--teal);
    line-height: 1.75;
    opacity: 0.9;
    flex: 1;
    margin: 0;
  }

  .category-works {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
    border-top: 1px solid rgba(15, 184, 184, 0.15);
    padding-top: 0.85rem;
  }

  .category-work-link {
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: 0.78rem;
    color: var(--teal);
    text-decoration: none;
    letter-spacing: 0.08em;
    opacity: 0.75;
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    transition: opacity 0.3s ease, color 0.3s ease;
  }

  .category-work-link .arrow {
    color: var(--yellow);
    transition: transform 0.3s ease;
  }

  @media (hover: hover) {
    .category-work-link:hover {
      opacity: 1;
      color: var(--yellow);
    }
    .category-work-link:hover .arrow {
      transform: translateX(0.2rem);
    }
  }

  .category-work-plain {
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: 0.78rem;
    color: var(--teal);
    letter-spacing: 0.08em;
    opacity: 0.5;
  }

  @media (max-width: 860px) {
    .categories {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }
  }

  /* === Portfolio === */
  .portfolio-section {
    position: relative;
  }

  .portfolio-section::before {
    content: "";
    position: absolute;
    top: 0;
    bottom: 0;
    left: calc(50% - 50vw);
    right: calc(50% - 50vw);
    background:
      radial-gradient(ellipse 70% 60% at 80% 30%, rgba(15, 184, 184, 0.08) 0%, transparent 65%),
      radial-gradient(ellipse 50% 50% at 15% 75%, rgba(255, 200, 87, 0.05) 0%, transparent 70%);
    z-index: -1;
    pointer-events: none;
  }

  .projects {
    list-style: none;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 3rem 4rem;
    margin: 2rem 0 0;
    padding: 0;
  }

  .project-item {
    list-style: none;
  }

  .project {
    position: relative;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    padding: 2rem 1.5rem 2rem 2rem;
    border-left: 1px solid rgba(15, 184, 184, 0.25);
    background: linear-gradient(180deg, rgba(15, 184, 184, 0.02) 0%, transparent 100%);
    transition: transform 0.5s ease, border-color 0.4s ease, background 0.4s ease;
    text-decoration: none;
    color: inherit;
  }

  .project-item:nth-child(1) .project { transform: translateY(-1rem); }
  .project-item:nth-child(2) .project { transform: translateY(2.5rem); }
  .project-item:nth-child(3) .project { transform: translateY(-3rem); }
  .project-item:nth-child(4) .project { transform: translateY(1.5rem); }

  @media (hover: hover) {
    a.project:hover {
      border-left-color: var(--yellow);
      background: linear-gradient(180deg, rgba(255, 200, 87, 0.05) 0%, transparent 100%);
    }

    .project-item:nth-child(1) a.project:hover { transform: translateY(-1.8rem); }
    .project-item:nth-child(2) a.project:hover { transform: translateY(1.8rem); }
    .project-item:nth-child(3) a.project:hover { transform: translateY(-3.8rem); }
    .project-item:nth-child(4) a.project:hover { transform: translateY(0.8rem); }
  }

  .project-locked {
    cursor: not-allowed;
    opacity: 0.65;
  }

  .project-num {
    position: absolute;
    top: 0.5rem;
    right: 1rem;
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: clamp(4rem, 8vw, 6rem);
    font-weight: 700;
    color: var(--yellow);
    line-height: 1;
    letter-spacing: -0.06em;
    opacity: 0.15;
    pointer-events: none;
    font-feature-settings: "tnum";
    transition: opacity 0.4s ease, transform 0.5s ease;
  }

  @media (hover: hover) {
    a.project:hover .project-num {
      opacity: 0.45;
      transform: translateY(-0.3rem);
    }
  }

  .project-category {
    display: inline-block;
    align-self: flex-start;
    margin-top: 0.75rem;
    padding: 0.3rem 0.7rem;
    font-family: "Noto Sans JP", "Source Han Sans", sans-serif;
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    color: var(--yellow);
    background: rgba(255, 200, 87, 0.08);
    border: 1px solid rgba(255, 200, 87, 0.3);
  }

  .project-name {
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: clamp(1.6rem, 3.2vw, 2.2rem);
    font-weight: 700;
    color: var(--white);
    letter-spacing: -0.04em;
    line-height: 1.1;
    margin-top: 0.6rem;
  }

  .project-tagline {
    font-family: "Noto Sans JP", "Source Han Sans", sans-serif;
    font-size: clamp(0.9rem, 1.4vw, 1.05rem);
    color: var(--teal);
    line-height: 1.7;
    opacity: 0.95;
    margin: 0;
  }

  .project-explore {
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: 0.78rem;
    color: var(--white);
    align-self: flex-start;
    letter-spacing: 0.18em;
    margin-top: 0.75rem;
    padding-bottom: 0.2rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.4);
    opacity: 0.55;
    transition: opacity 0.3s ease, color 0.3s ease, border-color 0.3s ease, gap 0.3s ease;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
  }

  .project-explore .arrow {
    color: var(--yellow);
    transition: transform 0.3s ease;
  }

  @media (hover: hover) {
    a.project:hover .project-explore {
      opacity: 1;
      color: var(--yellow);
      border-bottom-color: var(--yellow);
    }

    a.project:hover .project-explore .arrow {
      transform: translateX(0.25rem);
    }
  }

  .project-status {
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: 0.72rem;
    color: var(--teal);
    align-self: flex-start;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    margin-top: 0.75rem;
    padding: 0.3rem 0.75rem;
    border: 1px solid rgba(15, 184, 184, 0.3);
    opacity: 0.6;
  }

  @media (max-width: 720px) {
    .projects {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .project-item:nth-child(1) .project,
    .project-item:nth-child(2) .project,
    .project-item:nth-child(3) .project,
    .project-item:nth-child(4) .project {
      transform: none;
    }
  }

  /* === Company === */
  .company-details {
    display: flex;
    flex-direction: column;
    gap: 0;
    margin: 0;
    padding: 0;
  }

  .company-details > div {
    display: grid;
    grid-template-columns: 9rem 1fr;
    gap: 1.5rem;
    padding: 1.25rem 0;
    border-bottom: 1px solid rgba(15, 184, 184, 0.15);
  }

  .company-details > div:last-child {
    border-bottom: none;
  }

  .company-details dt {
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: 0.8rem;
    color: var(--yellow);
    letter-spacing: 0.15em;
    text-transform: uppercase;
    opacity: 0.85;
    margin: 0;
  }

  .company-details dd {
    font-family: "Noto Sans JP", "Source Han Sans", sans-serif;
    font-size: 1rem;
    color: var(--white);
    margin: 0;
    letter-spacing: -0.01em;
  }

  /* === Contact === */

  .contact-links {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin-top: 1rem;
  }

  .contact-links a {
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: 1.1rem;
    color: var(--white);
    text-decoration: none;
    border-bottom: 1px solid var(--white);
    padding-bottom: 0.25rem;
    letter-spacing: -0.01em;
    transition:
      color 0.2s ease,
      border-color 0.2s ease;
  }

  .contact-links a:hover {
    color: var(--yellow);
    border-bottom-color: var(--yellow);
  }

  /* === Footer === */
  .footer {
    padding: 6vh 8vw;
    border-top: 1px solid rgba(15, 184, 184, 0.15);
  }

  .footer-text {
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: 0.85rem;
    color: var(--teal);
    opacity: 0.6;
    letter-spacing: 0.02em;
  }

  /* === Hero curtain (open animation) === */
  /* === Hero curtain entrance === */
  .hero-curtain {
    position: fixed;
    inset: 0;
    z-index: 1000;
    background: var(--navy);
    transform-origin: top;
    animation: curtainOpen 1.3s cubic-bezier(0.77, 0, 0.18, 1) 0.3s forwards;
    pointer-events: none;
    will-change: transform;
  }

  @keyframes curtainOpen {
    0% { transform: scaleY(1); }
    100% { transform: scaleY(0); }
  }

  .hero-content > * {
    opacity: 0;
    transform: translateY(1.5rem);
    animation: heroFadeUp 1.1s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
  }

  .hero-content .brand { animation-delay: 1.1s; }
  .hero-content .rotator { animation-delay: 1.4s; }
  .hero-content .mission-hero { animation-delay: 1.7s; }
  .hero-content .brand-sub { animation-delay: 2.0s; }

  @keyframes heroFadeUp {
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .scroll-hint {
    opacity: 0;
    animation: bounce 2.4s ease-in-out 2.2s infinite, scrollHintIn 0.8s ease 2.2s forwards;
  }

  @keyframes scrollHintIn {
    to { opacity: 0.6; }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-curtain {
      animation-duration: 0.4s;
      animation-delay: 0s;
    }
    .hero-content > * {
      animation-duration: 0.3s;
      animation-delay: 0s;
    }
    .scroll-hint {
      animation: scrollHintIn 0.3s ease 0.4s forwards;
    }
  }

  /* === Stats === */
  /* === Architecture Layers === */
  .architecture-layers {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    margin: 1.5rem 0 0;
    padding: 0;
    counter-reset: layer-counter;
  }

  .layer {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
    padding: 1rem 0;
    border-top: 1px solid rgba(15, 184, 184, 0.18);
    transition: border-color 0.3s ease, transform 0.3s ease;
  }

  @media (hover: hover) {
    .layer:hover {
      border-top-color: var(--yellow);
      transform: translateX(4px);
    }
  }

  .layer-num {
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: 0.78rem;
    font-weight: 700;
    color: var(--yellow);
    letter-spacing: 0.12em;
    margin-right: 0.5rem;
    font-feature-settings: "tnum";
    opacity: 0.9;
  }

  .layer-name {
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: clamp(1.05rem, 1.7vw, 1.3rem);
    font-weight: 700;
    color: var(--white);
    letter-spacing: -0.01em;
    margin: 0;
    line-height: 1.4;
  }

  .layer-jp {
    font-family: "Noto Sans JP", "Source Han Sans", sans-serif;
    font-size: 0.78rem;
    font-weight: 400;
    color: var(--teal);
    margin-left: 0.6rem;
    letter-spacing: 0.05em;
    opacity: 0.85;
  }

  .layer-detail {
    font-family: "Noto Sans JP", "Source Han Sans", sans-serif;
    font-size: clamp(0.92rem, 1.4vw, 1rem);
    color: var(--teal);
    line-height: 1.75;
    opacity: 0.95;
    margin: 0;
  }

  .detail-closing {
    margin-top: 1.5rem;
    padding-top: 1.5rem;
    border-top: 1px solid rgba(15, 184, 184, 0.15);
    font-style: italic;
    opacity: 0.85;
  }

  /* === Hero grid === */
  .hero-grid {
    display: grid;
    grid-template-columns: 1fr 1.3fr;
    gap: 5vw;
    align-items: center;
    width: 100%;
  }

  .hero-visual {
    position: relative;
  }

  .hero-visual-img {
    width: 100%;
    aspect-ratio: 3 / 4;
    object-fit: cover;
    display: block;
    filter: saturate(1.05);
  }

  .hero-content {
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
    max-width: 55ch;
    position: relative;
    z-index: 1;
  }

  /* === Split layout (左右交互 sukima 流) === */
  .split-layout {
    max-width: 1080px !important;
    display: grid !important;
    grid-template-columns: 1fr 1fr;
    gap: 5vw;
    align-items: center;
  }

  .split-content {
    display: flex;
    flex-direction: column;
    gap: 1.75rem;
    min-width: 0;
  }

  .split-visual {
    position: relative;
    overflow: hidden;
    background: var(--navy);
    -webkit-mask-image: radial-gradient(
      ellipse 95% 95% at center,
      black 30%,
      rgba(0, 0, 0, 0.6) 70%,
      transparent 100%
    );
    mask-image: radial-gradient(
      ellipse 95% 95% at center,
      black 30%,
      rgba(0, 0, 0, 0.6) 70%,
      transparent 100%
    );
  }

  .split-visual::after {
    content: "";
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse at center, rgba(5, 16, 34, 0) 20%, rgba(5, 16, 34, 0.4) 100%);
    pointer-events: none;
    transition: opacity 0.8s ease;
  }

  @media (hover: hover) {
    .split-layout:hover .split-visual::after {
      opacity: 0.5;
    }
  }

  .split-visual-img {
    width: 100%;
    aspect-ratio: 4 / 5;
    object-fit: cover;
    display: block;
    opacity: 0.85;
    filter: saturate(0.7) brightness(0.85) contrast(1.05);
    transition: transform 0.8s ease, opacity 0.8s ease, filter 0.8s ease;
  }

  @media (hover: hover) {
    .split-layout:hover .split-visual-img {
      transform: scale(1.03);
      opacity: 1;
      filter: saturate(1) brightness(1) contrast(1);
    }
  }

  .split-layout.layout-left .split-content {
    order: 1;
  }

  .split-layout.layout-left .split-visual {
    order: 2;
  }

  .split-layout.layout-right .split-content {
    order: 2;
  }

  .split-layout.layout-right .split-visual {
    order: 1;
  }

  @media (max-width: 900px) {
    .hero-grid,
    .split-layout {
      grid-template-columns: 1fr !important;
      gap: 3rem;
    }

    .split-layout.layout-left .split-content,
    .split-layout.layout-left .split-visual,
    .split-layout.layout-right .split-content,
    .split-layout.layout-right .split-visual {
      order: 0;
    }

    .split-visual {
      display: none;
    }
  }

  /* === Side nav === */
  .side-nav {
    position: fixed;
    top: 50%;
    right: 2.5vw;
    transform: translateY(-50%);
    z-index: 100;
  }

  .side-nav ul {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 0.85rem;
    margin: 0;
    padding: 0;
  }

  .side-nav a {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    text-decoration: none;
    color: var(--teal);
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: 0.7rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    opacity: 0.45;
    transition:
      opacity 0.3s ease,
      color 0.3s ease,
      transform 0.3s ease;
  }

  .side-nav a::before {
    content: attr(data-num);
    font-feature-settings: "tnum";
    font-size: 0.65rem;
    opacity: 0.7;
  }

  .side-nav a:hover,
  .side-nav a.active {
    opacity: 1;
    color: var(--yellow);
    transform: translateX(-4px);
  }

  @media (max-width: 768px) {
    .side-nav {
      display: none;
    }
  }

  /* === Mobile menu (hamburger) === */
  .mobile-menu-toggle {
    display: none;
    position: fixed;
    top: 1.2rem;
    right: 1.2rem;
    z-index: 200;
    width: 48px;
    height: 48px;
    background: var(--navy);
    border: 1px solid rgba(15, 184, 184, 0.3);
    cursor: pointer;
    padding: 0;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 5px;
    transition: border-color 0.3s ease;
  }

  .mobile-menu-toggle:hover,
  .mobile-menu-toggle:focus-visible {
    border-color: var(--yellow);
    outline: none;
  }

  .mobile-menu-toggle .bar {
    display: block;
    width: 22px;
    height: 1.5px;
    background: var(--teal);
    transition: transform 0.3s ease, opacity 0.3s ease, background 0.3s ease;
  }

  .mobile-menu-toggle[aria-expanded="true"] .bar {
    background: var(--yellow);
  }

  .mobile-menu-toggle[aria-expanded="true"] .bar:nth-child(1) {
    transform: translateY(6.5px) rotate(45deg);
  }

  .mobile-menu-toggle[aria-expanded="true"] .bar:nth-child(2) {
    opacity: 0;
  }

  .mobile-menu-toggle[aria-expanded="true"] .bar:nth-child(3) {
    transform: translateY(-6.5px) rotate(-45deg);
  }

  .mobile-menu {
    position: fixed;
    inset: 0;
    background: rgba(5, 16, 34, 0.97);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    z-index: 150;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.4s ease;
    display: flex;
    flex-direction: column;
    justify-content: center;
    padding: 8vh 8vw;
  }

  .mobile-menu[aria-hidden="false"] {
    opacity: 1;
    pointer-events: auto;
  }

  .mobile-menu ul {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    padding: 0;
    margin: 0;
  }

  .mobile-menu a {
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: clamp(1.8rem, 7vw, 2.6rem);
    color: var(--white);
    text-decoration: none;
    font-weight: 700;
    letter-spacing: -0.02em;
    display: flex;
    align-items: baseline;
    gap: 1rem;
    transition: color 0.3s ease, transform 0.3s ease;
  }

  .mobile-menu a::before {
    content: attr(data-num);
    font-size: 0.85rem;
    color: var(--yellow);
    font-weight: 700;
    letter-spacing: 0.15em;
    opacity: 0.7;
    font-family: "Futura", "Helvetica Neue", sans-serif;
  }

  .mobile-menu a:active,
  .mobile-menu a:hover {
    color: var(--yellow);
    transform: translateX(8px);
  }

  @media (max-width: 768px) {
    .mobile-menu-toggle {
      display: flex;
    }
  }

  @media (min-width: 769px) {
    .mobile-menu {
      display: none;
    }
  }

  /* === Hero rotator === */
  .rotator {
    position: relative;
    height: clamp(3rem, 8vw, 6rem);
    width: 100%;
    overflow: hidden;
  }

  .rotator-word {
    position: absolute;
    top: 0;
    left: 0;
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: clamp(2.2rem, 6vw, 4.5rem);
    font-weight: 700;
    color: var(--yellow);
    letter-spacing: -0.03em;
    line-height: 1;
    opacity: 0;
    transform: translateY(20px);
    animation: rotateWords 21s linear infinite;
  }

  .rotator-word:nth-child(1) { animation-delay: 0s; }
  .rotator-word:nth-child(2) { animation-delay: 1.5s; }
  .rotator-word:nth-child(3) { animation-delay: 3s; }
  .rotator-word:nth-child(4) { animation-delay: 4.5s; }
  .rotator-word:nth-child(5) { animation-delay: 6s; }
  .rotator-word:nth-child(6) { animation-delay: 7.5s; }
  .rotator-word:nth-child(7) { animation-delay: 9s; }
  .rotator-word:nth-child(8) { animation-delay: 10.5s; }
  .rotator-word:nth-child(9) { animation-delay: 12s; }
  .rotator-word:nth-child(10) { animation-delay: 13.5s; }
  .rotator-word:nth-child(11) { animation-delay: 15s; }
  .rotator-word:nth-child(12) { animation-delay: 16.5s; }
  .rotator-word:nth-child(13) { animation-delay: 18s; }
  .rotator-word:nth-child(14) { animation-delay: 19.5s; }

  /* 1語の可視窓は 7%（=1.47s）以内に収める。1.5s 間隔 × 14語 = 21s 周期で重なりゼロ */
  @keyframes rotateWords {
    0% {
      opacity: 0;
      transform: translateY(20px);
    }
    1.5% {
      opacity: 1;
      transform: translateY(0);
    }
    5% {
      opacity: 1;
      transform: translateY(0);
    }
    6.8% {
      opacity: 0;
      transform: translateY(-20px);
    }
    100% {
      opacity: 0;
      transform: translateY(-20px);
    }
  }

  /* === Mobile responsive === */
  @media (max-width: 640px) {
    section {
      padding: 8vh 6vw;
    }

    .hero {
      padding: 6vh 6vw;
      gap: 2.5rem;
      min-height: 90vh;
    }

    .core-words {
      gap: 0.75rem 1.25rem;
    }

    .philosophy {
      gap: 1.5rem;
    }

    .members {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .commitments {
      gap: 2rem;
    }

    .projects {
      gap: 1.5rem;
    }

    .footer {
      padding: 5vh 6vw;
    }
  }

  .brand {
    font-family: "Futura", "Futura PT", "Jost", "Helvetica Neue", sans-serif;
    font-weight: 700;
    font-size: clamp(3.5rem, 14vw, 10rem);
    color: var(--white);
    letter-spacing: -0.06em;
    line-height: 0.95;
    transform: scaleY(1.1);
    transform-origin: left bottom;
    display: inline-block;
    mix-blend-mode: difference;
    opacity: 0;
    animation: brandEntrance 1.2s cubic-bezier(0.2, 0.6, 0.2, 1) 1.5s forwards;
  }

  @keyframes brandEntrance {
    0% {
      opacity: 0;
      transform: scaleY(1.1) translateY(20px);
    }
    100% {
      opacity: 1;
      transform: scaleY(1.1) translateY(0);
    }
  }

  .mission {
    font-family: "Futura", "Helvetica Neue", sans-serif;
    font-size: clamp(1.2rem, 3vw, 2rem);
    font-weight: 700;
    color: var(--teal);
    max-width: 38ch;
    line-height: 1.4;
    letter-spacing: -0.01em;
  }

  .core-words {
    display: flex;
    flex-wrap: wrap;
    gap: 1.5rem 2.5rem;
    max-width: 50ch;
  }

  .core-word {
    font-family: "Futura", "Helvetica Neue", sans-serif;
    color: var(--yellow);
    font-style: italic;
    letter-spacing: 0.02em;
  }

  .core-word:nth-child(1) {
    transform: translateY(0);
    opacity: 0.95;
    font-size: 1rem;
  }
  .core-word:nth-child(2) {
    transform: translateY(0.5rem);
    opacity: 0.7;
    font-size: 0.85rem;
  }
  .core-word:nth-child(3) {
    transform: translateY(-0.3rem);
    opacity: 0.85;
    font-size: 1.1rem;
  }
  .core-word:nth-child(4) {
    transform: translateY(0.8rem);
    opacity: 0.75;
    font-size: 0.9rem;
  }
  .core-word:nth-child(5) {
    transform: translateY(-0.2rem);
    opacity: 0.9;
    font-size: 1.05rem;
  }
  .core-word:nth-child(6) {
    transform: translateY(0.4rem);
    opacity: 0.8;
    font-size: 0.95rem;
  }
  .core-word:nth-child(7) {
    transform: translateY(-0.5rem);
    opacity: 0.95;
    font-size: 1.15rem;
  }

  /*
    引き算指示書 8 ヶ条 (リマインダー):
    - Explanation is a bug.
    - Kill the "Kindness".
    - Substance, not Style.
    - Empty space is the Message.
    - Design is Deletion.
    - If you can remove it, do it.
    - Don't guide. Just be.
    - Noise is the Enemy.
  */
</style>
