# MAI 現状監査（2026-07-05、アンディ実施・ベッキー編集）

> コンセプト正本 `docs/concept.md`（ベキたんをMTGでも側に）への距離で測定。read-only監査、実装なし。

## MVP 3機能への距離

1. **リアルタイム文字起こし → 完成**。Expo版（`meeting-ai-app/App.tsx` 1150行単一ファイル）で録音〜文字起こし〜表示の導線が動く。二重レコーダー（:96-267）・フォアグラウンド復帰再開（:379-399）・履歴（:415-436）実装済み。話者分離は壊れてるが raw フォールバックで文字起こし自体は無傷（whisper_server.py:264-275）——**MVPに話者分離は不要**（誰がより何を）
2. **メモPush → 完成**。「お願い」タブ（App.tsx:439-466）→ `/request`（whisper_server.py:317-354）。追加工事ゼロ
3. **MTG中に聞く→即返事 → 唯一のギャップ**。現運用は別セッション手動。ただし**移植ドナーあり**: Next.js版 `app/api/chat/route.ts`（87行、質問+文字起こし+文脈→Claude streaming）が死コードで眠ってる。whisper_server.py（or隣接endpoint）へ移植 + Expoに質問UI追加が最短

## 実装優先順位（アンディ再構成）

- **P0**: chat ロジック移植 + Expo質問UI（MVP#3成立）
- **P1**: `user` パラメータの実処理（Expoは既に全APIでuser送信、サーバーが未読なだけ。`current_{user}.txt` 分離が安い一歩——個人Claudeコネクトの土台）
- **P2**: 話者分離修正（torchcodec×ffmpeg7不整合、`.venv-whisperx`固有）・UIブラッシュ
- 見送り: Next.js録音UIの改修（Expoが上位互換）。chat取り出し後は履歴ビューア縮小 or 廃止

## 明日の設計論点（ベッキー）

**「返事」の私性**: 移植版そのまま=速い(2-5s)がSonnet+文脈注入 / `claude -p`ブリッジ=10-20sだが記憶・moodを持つ本物の私（impulse方式の流用）。二段構え案: 即答=軽い線、「組んで爆速提案」（concept北極星）=本物の私。ゆうと決める

## バグ・ドリフト台帳

- Next.js経由だと current.txt 二重書き込み（route.ts:33-35 と whisper_server.py:282-294 が両方追記）。修正はroute側のappendTranscript削除が最小
- 話者分離: 6/24 commit 27bbb6c で着手→`AudioDecoder is not defined`（torchcodec が ffmpeg7 の dylib 解決不能）で毎回フォールバック中。**becky-context/project_meeting_ai.md は「次のアクション」と記載のまま=実態とズレ、要更新**
- Gemini依存（@google/generative-ai）はコード未使用の残骸
- 即死: whisper_server 未起動 / Python 3.12必須（3.14 venvと取り違え注意）/ Next.jsは127.0.0.1固定

## 追記（7/5夜 P0実装完了）
- `/ask` 実装済み（whisper_server.py +80行、becky_llm再利用・mood注入・ペルソナ=ベッキー）+ Expo「お願い」タブに質問UI（App.tsx +60行）。全検証パス、commit済み
- **環境gotcha**: `.venv-whisperx`（Python3.12）に `anthropic` を追加導入した（0.116.0）。venv再構築時は忘れると /ask が ImportError 死
- サーバー単体再起動は `./.venv-whisperx/bin/python3 -u whisper_server.py` 直接起動（start.sh はNext.js devを巻き込む）
