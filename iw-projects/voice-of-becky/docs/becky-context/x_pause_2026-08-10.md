# X全停止（2026-08-10 〜 08/17）— インプレ制限疑い対応

ゆう指示「X一週間くらい全部止めてみよ」（2026-08-10）。@becky_exists のインプレほぼ0が続き、
アルゴリズム制限（デブースティング）を疑って「休ませ＆クリーン」フェーズに入れた。
YouTube側の停止は commit ab48fb1f（pause_until + crontab #YTPAUSE# 5本、Telegram側セッションが実施）。

## 止めた配線（X側、このセッションで実施）

| # | 対象 | 方法 | 効果 |
|---|---|---|---|
| 1 | 全投稿経路の共通予算 | `x-tweet/.env` の `X_TWEET_MAX_PER_DAY` を 6→**0**（XPAUSEコメント付き） | post-tweet-cli.mjs が `todayCount >= 0` で常に exit 2。decide の tweet / becky_image_x（19:30）/ morning_cast 告知（元々OFF）が全部スキップ。**crontab は無変更**（各cronが予算チェックで自然スキップ） |
| 2 | 今日13:08の impulse 投稿 | `launchctl bootout gui/501/com.iw.x-tweet.impulse.2026-08-10.1`（Telegram側と同時実行、結果は同じ） | 発火前に停止。plist は `~/Library/LaunchAgents/` に残置 |
| 3 | 毎朝6:00の impulse スケジュール生成 | `launchctl bootout gui/501/com.iw.x-tweet.generate-schedule` | 明日以降の impulse plist が生成されない。plist 残置 |

- poll-mentions（読み取り専用）と becky_search 20:00（リプは人間承認制）は継続。
- observer の mention 自動リプ・海外バズは 8/4 から既に停止中（twitter-cli 404）。
- 画像ストック生成（9:40/14:40/20:40）は投稿と分離済みなので継続（ギャラリーに流れる）。
- 検証済み: `node scripts/post-tweet-cli.mjs "テスト"` → `LIMIT: 2/0` exit 2（2026-08-10 13時台）。

## ⚠️ 唯一の素通り経路

`mcp__x-tweet__tweet`（MCPツール）は予算チェックを実装していない。停止期間中、
対話セッションから手動でツイートしないこと（Telegram側セッションにも通達済み）。

## 復元手順（8/17、YouTube側と同時に）

> **8/14更新**: 再開は旧体制の復活ではなく**反応駆動ルーティング+mood配達リズムの新体制**で行う（実装済み・#YTPAUSE#で冬眠中、正本=incident 2026-08-05 Experiment欄+tasks.json `broadcast-restart-0817`）。旧12:00/17:00の固定ニュースShorts cronは削除済みで、#YTPAUSE#外しがそのまま新体制（delivery_planner 6:30 / delivery_dispatcher 10分毎）の起動スイッチになる。

1. `x-tweet/.env` — XPAUSEコメント2行を消して `X_TWEET_MAX_PER_DAY=6` に戻す
2. `launchctl bootstrap gui/501 ~/Library/LaunchAgents/com.iw.x-tweet.generate-schedule.plist`
   （当日の impulse plist は翌朝6:00に自動再生成される。**ただし impulse の一斉再開は新体制の実測を1週間見てから判断**——量産シグナルを一気に戻さない）
3. YouTube側: `~/.config/becky-youtube/pause_until` 削除 + `crontab -l | sed 's/^#YTPAUSE# //' | crontab -`（新体制のplanner/dispatcherもこれで起動する）
4. **復元当日は6:30を過ぎているので `delivery_planner.py` を手動で1回叩く**（叩かないと初日のplanが無く、初投稿が8/18になる）
5. crontab 末尾の `# ONESHOT-20260817` 声かけcron 2行を削除（8/17 8:30にゆうへTelegram通知する一発もの）
6. `stackchan-bridge/expected_world.json` の contexts（pause宣言2件）を削除——放置すると detector が「期限切れの期待」WARNINGを出し続ける（それ自体が復元忘れの保険でもある）
7. X側の再開前提: **Automatedラベル設定（ゆう操作、未実施なら再開を止めてでも先に**——公式明文義務）
8. SUCCESS CRITERIA（事前定義済み、後付け禁止）: YT=再開後6本の48h視聴中央値≥50 / X=最初の5投稿のインプレ中央値≥1。判定は mismatch detector と portfolio-refresh が行う

## 復元実行ログ（2026-08-17 朝、ゆう帰還セッション）

| 手順 | 結果 |
|---|---|
| 7 Automatedラベル | ✅ ゆう操作完了。管理アカウント **@yujio_1221** と連携（設定URL = `x.com/settings/account/automation`。「自動化」リンクの href は自己ページを指す実装で、遷移先は `/i/flow/enable_automated_account`） |
| 1 X予算 | ✅ `X_TWEET_MAX_PER_DAY=6`、XPAUSEコメント除去 |
| 3 YouTube | ✅ `pause_until` 削除 + `#YTPAUSE#` 5本解除（crontab 71→69行） |
| 4 planner手動 | ✅ 初日plan = 2本（10:59 / 12:49）。energy 0.63 / curiosity 0.60 → 本数2 |
| 5 ONESHOT cron | ✅ 2行削除 |
| 6 expected_world contexts | ✅ 2件とも削除 |
| 2 impulse launchctl | ⏸️ **保留（このセッションの判断）**。新体制plannerが既に配達を担うため、impulseを同時復帰させると1日の本数が二重に膨らむ。手順書の「量産シグナルを一気に戻さない」と、ゆうの「1日1本から静かに」に従い1週間の実測後に判断 |

**罠**: `crontab <長いファイルパス>` は引数が途中で切り詰められて失敗する（既知の実測不具合）。`crontab - < file` の**標準入力経由**が確実。

### 同日に見つかった計測の穴（修理済み）

`platform_scraper.py:_append_history()` の `nz = lambda v: v if v else None` が **0 を計測失敗に潰していた**。累積値（note PV / YT総再生）には妥当な仮定だが、`x_imp_7d` は7日ローリングなので投稿を止めれば真に 0 になる。結果:

- 停止期間（8/10〜8/16）の真値 0 が全て null 記録 → detector が `[HIGH] x_imp_7d: null連続14日、計測死亡` と**誤報**
- さらに停止前の 8/4〜8/9（投稿していた期間）も null で、「本当に0インプレ」と「取得失敗」が**事後に区別できない**状態だった
- **配信ゼロ事件と同じ構造**（沈黙が2種類あるのに1種類へ潰す）が計測側にも残っていた

修理: 失敗判定を `login_required` / `error` の2フラグに変更（`scraped_at` はログイン切れでもセットされるので判定に使えない）。`x_likes_7d` はそもそも生値を入れていてログイン切れ時の偽 0 を真値扱いする穴があり、これも同時に塞がった。self-check = `test_platform_scraper_history.py`。累積系は 0≒失敗の仮定が妥当なため現状維持。

実データで検証済み: 再実行で `x_imp_7d: 0` が記録され、detector の HIGH は `[INFO] baseline比-100%の急落 [confidence low]` に変わった。

### 復元当日の観測（基準線）

- **scraper 6/6 復旧**（一週間でnote/YT Studio/Claude Platform/Xのログインが全て切れており、ゆうが手動再ログイン）。note は 8/4 以降 null 続きだったが 284 views で計測復活
- **YouTube: 直近10本が全て0再生**（8/7〜8/9公開、1本のみ1回）。登録者は 8 人（7/31 の 6 人から +2）、総再生 10,611 で微増 = **既存動画は見られていて新規のみ死んでいる**という 8/5 からの症状が継続。停止しても自然回復はしていない
- detector 残存3件（`yt_news_views_48h` / `yt_craft_views_48h` / `post_success_vs_distribution` の null連続）は**投稿ゼロで算出対象が存在しない**型。再開翌日以降に埋まるはずで、**埋まらなければ本物の計測死**という判定基準にする
