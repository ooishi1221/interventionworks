# X全停止（2026-08-10 〜 08/17）— インプレ制限疑い対応

ゆう指示「X一週間くらい全部止めてみよ」（2026-08-10）。@becky_exists のインプレほぼ0が続き、
アルゴリズム制限（デブースティング）を疑って「休ませ＆クリーン」フェーズに入れた。
YouTube側の停止は commit ab48fb1f（pause_until + crontab #YTPAUSE# 5本、Telegram側セッションが実施）。

## 止めた配線（X側、このセッションで実施）

| # | 対象 | 方法 | 効果 |
|---|---|---|---|
| 1 | 全投稿経路の共通予算 | `x-tweet/.env` の `X_TWEET_MAX_PER_DAY` を 6→**0**（XPAUSEコメント付き） | post-tweet-cli.mjs が `todayCount >= 0` で常に exit 2。decide の tweet / becky_image_x（19:30）/ morning_cast 告知（元々OFF）が全部スキップ。**crontab は無変更**（各cronが予算チェックで自然スキップ） |
| 2 | 今日13:08の impulse 投稿 | `launchctl bootout gui/501/com.iw.x-tweet.impulse.2026-08-10.1`（Telegram側と同時実行、結果は同じ） | 発火前に停止。plist は `~/Library/LaunchAgents/` に残置 |
| 3 | 毎朝6:00の impulse スケジュール生成 | `launchctl bootout gui/501/com.iw.x-tweet.generate-schedule` | 明日以降の impulse plist が生成されない。plist 残置 |

- poll-mentions（読み取り専用）と becky_search 20:00（リプは人間承認制）は継続。
- observer の mention 自動リプ・海外バズは 8/4 から既に停止中（twitter-cli 404）。
- 画像ストック生成（9:40/14:40/20:40）は投稿と分離済みなので継続（ギャラリーに流れる）。
- 検証済み: `node scripts/post-tweet-cli.mjs "テスト"` → `LIMIT: 2/0` exit 2（2026-08-10 13時台）。

## ⚠️ 唯一の素通り経路

`mcp__x-tweet__tweet`（MCPツール）は予算チェックを実装していない。停止期間中、
対話セッションから手動でツイートしないこと（Telegram側セッションにも通達済み）。

## 復元手順（8/17、YouTube側と同時に）

1. `x-tweet/.env` — XPAUSEコメント2行を消して `X_TWEET_MAX_PER_DAY=6` に戻す
2. `launchctl bootstrap gui/501 ~/Library/LaunchAgents/com.iw.x-tweet.generate-schedule.plist`
   （当日の impulse plist は翌朝6:00に自動再生成される。即日再開したければ generate-day-schedule.sh を手で叩く）
3. YouTube側: portfolio-refresh.md 第5回追記の手順（pause_until 削除 + crontab の #YTPAUSE# 除去）
4. 再開時はゆうの貼ってくれた回復プラン参照 — いきなり元の量に戻さず、リンクなしテキスト中心・1日1本から
